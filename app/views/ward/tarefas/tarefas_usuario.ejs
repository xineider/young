
  		<ul class="collapsible popout" data-collapsible="accordion, expandable">
        <%
        if (typeof data != 'undefined' && data.length > 0) {
          for (var i = data.length - 1; i >= 0; i--) {
        %>
        <li>
		      <div class="collapsible-header position TarefaLoad" data-id="<%= data[i].id %>" style="padding-right: 100px;">
            <div class="porcentagem" style="position: absolute;bottom: 0;left: 0;background-color: rgb(79, 33, 40);width: <%= data[i].porcentagem * 100 %>%;height: 5px;transition: 1s;"></div>
            <span class="texto">
		      	 <%= data[i].nome %>&nbsp;|&nbsp;<b>Data de Entrega:</b>&nbsp;<%= data[i].data_prevista %>&nbsp;<b>Tempo disponivel:</b>&nbsp;<%= data[i].tempo_previsto.slice(0, 5) %>/horas
            </span>
		      	<span class="right-position">
		      		<i class="fa fa-times-circle red-text modal-remover-mount" data-href="#modaldeletar" data-id="<%= data[i].id %>" data-texto="Você tem certeza que quer deletar '<%= data[i].nome %>'? " data-to="/sistema/tarefas/desativar" data-back="/sistema/tarefas/tarefas" aria-hidden="true"></i>
		      		<!-- <i class="fa fa-comments-o blue-text" aria-hidden="true"></i> -->
		      	</span>
		      </div>
		      <div class="collapsible-body">
		      	<ul class="collection with-header tarefas">
			      </ul>
		      </div>
		    </li>
        <%
          }
        } else {
        %>
        <li>
          <div class="collapsible-header position">
            Não foi cadastrada nenhuma tarefa até o momento
          </div>
        </li>
        <%
        }
        %>
		  </ul>
