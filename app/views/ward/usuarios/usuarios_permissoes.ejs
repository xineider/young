<a href="/sistema/usuarios/criar" class="ajax-load">
  <div class="cream darken-3 tab-archive tab-nivel-2 tab-nivel-2-l">Criar Usuários</div>
</a>
<div class="cream tab-archive">Pesquisar/Editar</div>


<% if(usuario.nivel == 1){%>

<div class="card cream archive">

  <div class="header row">
    <div class="right valign-wrapper">
      <div class="black-text margin-r-5">Criar Usuário</div>
      <a class="btn-floating btn-large waves-effect waves-light bordo right ajax-load" href="/sistema/usuarios/criar"><i class="fa fa-plus" aria-hidden="true"></i></a>
    </div>
  </div>

  <div class="card" style="max-height: 50%">
    <div class="pane" style="padding-top:10px;">

          <table class="centered highlight responsive-table cell-border" id="tabela_interna_adverso" style="width:100%;">
            <thead>
              <tr>
                <th>Login</th>
                <th>Nome</th>
                <th>Email</th>
                <th>Telefone</th> 
                <th>Nível</th>
                <th>Setor</th>    
              </tr>
            </thead>
            <tbody>
              <%
              if (typeof data != 'undefined' && data.length > 0) {
                for (var i = data.length - 1; i >= 0; i--) {
                  %>
                  <tr>
                    <td>
                      <%= data[i].login %>
                    </td>
                    <td>
                      <%= data[i].nome %>
                    </td>
                    <td>
                      <%= data[i].email %>
                    </td>
                    <td>
                      <%= data[i].telefone %>
                    </td>
                    <td>     

                      <% switch(data[i].nivel){
                        case 1:%>
                        Administrador
                        <% break;
                        case 2:%>
                        Gerente
                        <%break;
                        case 3:%>
                        Usuário
                        <%break;}%>

                      </td>
                      <td>
                        <%= data[i].setor %>                        
                      </td>
                    </tr>

                    <%
                  }
                }
                %>

              </tbody>
            </table>
          </div>
        </div>
      </div>

      <%}else{%>
      <div class="card cream archive">
        <p>Sinto Muito, você não possuí autoridade para visualizar esta página</p>
        <a href="/sistema">Clique aqui para voltar</a>
      </div>


      <%}%>